syntax = "proto3";

// Assign a package name. This will appear as C++ namespace later
package proto_messages;

// Creates a message "HelloWorld", that holds the fields "name", "id" and "msg".
message HelloWorld
{
  string name      = 1;
  uint32 id        = 2;
  string msg       = 3;
}
